#!/bin/bash

git add . && \
git commit -m "msg" && \
git push

#<p><strong>phase</strong><\p>    ==> used to emphasize keywords inside <p>
#<pre><code> </code></pre> ==> used for inserting codes with black background
#<blockquote><\blockquote> ==> used to quoate with gray background